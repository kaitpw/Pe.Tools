<core:RevitHostedUserControl x:Class="PeUi.Components.ListView"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                             xmlns:local="clr-namespace:PeUi.Components"
                             xmlns:core="clr-namespace:PeUi.Core"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>


            <!-- ScrollViewer Style with WPF.UI DynamicScrollBar -->
            <Style x:Key="CustomScrollViewerStyle" TargetType="ScrollViewer">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollViewer">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ScrollContentPresenter Grid.Column="0"
                                                        Content="{TemplateBinding Content}"
                                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                                        CanContentScroll="{TemplateBinding CanContentScroll}" />

                                <ui:DynamicScrollBar x:Name="PART_VerticalScrollBar"
                                                     Grid.Column="1"
                                                     Orientation="Vertical"
                                                     ViewportSize="{TemplateBinding ViewportHeight}"
                                                     Maximum="{TemplateBinding ScrollableHeight}"
                                                     Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                                     Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                                     Cursor="Arrow" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ListViewItemStyle" TargetType="{x:Type ui:ListViewItem}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="6,2,6,2" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ui:ListViewItem}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}"
                                        CornerRadius="4">
                                    <ContentPresenter
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                </Border>

                                <Separator Grid.Row="1"
                                           Margin="6,0,6,0"
                                           Background="{ui:ThemeResource DividerStrokeColorDefaultBrush}"
                                           Height="1" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <!-- Hover state -->
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{ui:ThemeResource SubtleFillColorSecondaryBrush}" />
                    </Trigger>
                    <!-- Selected state -->
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{ui:ThemeResource SubtleFillColorSecondaryBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <ui:ListView x:Name="ItemListView"
                 ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=local:ListView}}"
                 SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=local:ListView}, Mode=TwoWay}"
                 SelectedIndex="{Binding SelectedIndex, RelativeSource={RelativeSource AncestorType=local:ListView}, Mode=TwoWay}"
                 SelectionChanged="ItemListView_SelectionChanged"
                 MouseLeftButtonUp="ItemListView_MouseLeftButtonUp"
                 MouseRightButtonUp="ItemListView_MouseRightButtonUp"
                 MouseMove="ItemListView_MouseMove"
                 MouseLeave="ItemListView_MouseLeave"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 HorizontalContentAlignment="Stretch"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 ItemContainerStyle="{StaticResource ListViewItemStyle}">
        <ui:ListView.Resources>
            <Style TargetType="ScrollViewer" BasedOn="{StaticResource CustomScrollViewerStyle}" />
        </ui:ListView.Resources>
    </ui:ListView>
</core:RevitHostedUserControl>